export enum CSS_DISPLAY {
  FLEX = 'flex',
  GRID = 'grid',
  NONE = 'none',
  BLOCK = 'block',
  INLINE_BLOCK = 'inline-block',
}

export enum CSS_JUSTIFY_CONTENT {
  CENTER = 'center',
  SPACE_BETWEEN = 'space-between',
  SPACE_AROUND = 'space-around',
  START = 'flex-start',
  END = 'flex-end',
  LEFT = 'left',
  RIGHT = 'right',
}

export enum CSS_FLEX_WRAP {
  WRAP = 'wrap',
  NOWRAP = 'nowrap',
  WRAP_REVERSE = 'wrap-reverse',
}

export enum CSS_FLEX_DIRECTION {
  ROW = 'row',
  COLUMN = 'column',
}

export enum CSS_ALIGN {
  CENTER = 'center',
  LEFT = 'left',
  RIGHT = 'right',
  JUSTIFY = 'justify',
}

export enum CSS_TEXT_DECORATION {
  NONE = 'none',
}

export enum CSS_COLOR {
  WHITE = '#FFFFFF',
  BLACK = '#000000',
}
